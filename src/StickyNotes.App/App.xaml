<Application x:Class="StickyNotes.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <FontFamily x:Key="AppFont">Segoe UI Variable, Segoe UI</FontFamily>

            <!-- Chrome custom estilo Windows 11: cantos arredondados + borda sutil,
                 mantendo sombra, resize e snap nativos do sistema. O raio é o mesmo
                 das superfícies internas, para os cantos aninhados concordarem. -->
            <WindowChrome x:Key="ModernChrome"
                          CaptionHeight="40"
                          ResizeBorderThickness="6"
                          CornerRadius="12"
                          GlassFrameThickness="0"
                          UseAeroCaptionButtons="False"/>

            <!-- FluentButtonStyle e os estilos de controle vivem em
                 Resources/Styles.xaml — uma única definição por papel. -->

            <Style TargetType="RadioButton">
                <Setter Property="FontFamily" Value="{StaticResource AppFont}"/>
                <Setter Property="FontSize" Value="12.5"/>
                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                <Setter Property="Margin" Value="0,0,14,0"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
