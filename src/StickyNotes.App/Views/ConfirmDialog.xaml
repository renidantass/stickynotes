<Window x:Class="StickyNotes.Views.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Confirmação"
        Icon="../Resources/StickyNotes.ico"
        Width="400"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        Background="{DynamicResource SurfaceElevatedBrush}"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        TextOptions.TextFormattingMode="Ideal"
        KeyDown="OnKeyDown">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            ResizeBorderThickness="0"
                            CornerRadius="16"
                            GlassFrameThickness="0"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <!-- Plano de atenção: a superfície mais elevada do app. Raio maior e
         superfície própria para deixar claro que está acima do conteúdo. -->
    <Border x:Name="DialogSurface"
            Background="{DynamicResource SurfaceElevatedBrush}"
            BorderBrush="{DynamicResource SeparatorBrush}"
            BorderThickness="1"
            Padding="22,20">
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,18">
                <Border Width="36" Height="36"
                        CornerRadius="{StaticResource RadiusPill}"
                        Background="{DynamicResource DangerBackgroundBrush}"
                        VerticalAlignment="Top"
                        Margin="0,0,13,0">
                    <TextBlock Text="&#xE7BA;"
                               FontFamily="Segoe Fluent Icons"
                               FontSize="15"
                               Foreground="{DynamicResource DangerBrush}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Excluir nota"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <TextBlock x:Name="MessageText"
                               TextWrapping="Wrap"
                               FontSize="12.5"
                               LineHeight="18"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               Margin="0,5,0,0"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancelar"
                        Style="{StaticResource SecondaryActionButtonStyle}"
                        MinWidth="92"
                        Margin="0,0,8,0"
                        IsCancel="True"
                        Click="OnCancelClick"/>
                <Button x:Name="ConfirmButton"
                        Content="Excluir"
                        Style="{StaticResource DestructiveActionButtonStyle}"
                        MinWidth="92"
                        IsDefault="True"
                        Click="OnConfirmClick"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
